<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to "/", class: "navbar-brand" do %>
    <%= image_tag "tapas_logo.png", class: "logo_navbar" %>
  <% end %>

  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto"> <!-- Changed mr-auto to me-auto for Bootstrap 5 -->
      <% if admin_types.include?(current_user.try(:type)) %>
        <%= render 'shared/admin_add_ao' %>
      <% end %>
      <li class="nav-item">
        <%= link_to "LISTE DES AO", posts_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "FAVORIS", favorites_path, class: "nav-link" %>
      </li>
      <li class="nav-item dropdown">
        <%= image_tag "avatar.png", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown"> <!-- Changed right to end -->
          <% if admin_types.include?(current_user.try(:type)) %>
            <%= render 'shared/admin_dashboard' %>
          <% end %>
          <%= link_to "DÉCONNEXION", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
        </div>
      </li>
    </ul>
  </div>
</div>
